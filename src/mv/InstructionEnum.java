package mv;

/**
 * Conjunto de las instrucciones del repertorio.
 * @author Samuel Lapuente y Héctor Laria.
 *
 */
public enum InstructionEnum {
	PUSH, POP, DUP, FLIP, LOAD, STORE, ADD, SUB, MUL, DIV, OUT, HALT;
}
